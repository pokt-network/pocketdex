vendors:
  - name: "@cosmjs"
    path: "vendor/cosmjs"
    workspace: true
    install:
      required: true
      script: "yarn install"
      cleanCache: true
    build:
      required: true
      script: "yarn build"
    pack:
      includes:
        - "stargate"
        - "tendermint"

  - name: "@subql"
    path: "vendor/subql"
    workspace: true
    install:
      required: true
      script: "yarn install"
      cleanCache: true
    build:
      required: true
      script: "yarn build"
    dependencies:
      - "@cosmjs"
    pack:
      includes:
        - "node-core"

  - name: "@subql-cosmos"
    path: "vendor/subql-cosmos"
    workspace: true
    install:
      required: true
      script: "yarn install"
      cleanCache: true
    build:
      required: true
      script: "yarn build"
    packageResolution:
      "node-cosmos": "node"
      "types-cosmos": "types"
    dependencies:
      - "@cosmjs"
      - "@subql"
